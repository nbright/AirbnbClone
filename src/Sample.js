import React from 'react';
import { View, Text } from 'react-native';

const Home = props => {
    return (
        <View>
            <Text>Hi</Text>
        </View>
    );
};

export default Sample;
